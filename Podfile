# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PhotoPrint UI' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PhotoPrint UI
pod 'PhotoLibrary', :git => 'https://github.com/GalaxyDevGamer/PhotoLibrary.git'
  target 'PhotoPrint UITests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PhotoPrint UIUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
